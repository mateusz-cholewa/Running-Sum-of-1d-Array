class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        result = []
        result.append(nums[0])
        for num in range(1, len(nums)):
            result.append(output[-1]+nums[num])
        return result
        
